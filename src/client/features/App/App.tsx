import React from "react";

export interface AppProps{

}

export const App: React.FunctionComponent<AppProps> = ({
}) => {
    return (
        <div>
            app
        </div>
    );
};

export default App;